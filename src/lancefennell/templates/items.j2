<main>
    {% for item in items %}
        <figure>
            <img src="{{ item.index_img_href }}" />
            <figcaption>
                <a href="{{ url_for('detail', slug=item.slug) }}">{{ item.title }}</a>
            </figcaption>
        </figure>
    {% endfor %}
</main>