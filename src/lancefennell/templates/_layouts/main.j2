<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="shortcut icon" href="{{ url_for('static', filename='favicon.ico') }}">
        <title>{% block title %}{{ config.SITE_TITLE }}{% endblock %}</title>
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li><a href="{{ url_for('items', category='studio') }}">Studio</a></li>
                    <li><a href="{{ url_for('items', category='paintings') }}">Paintings</a></li>
                </ul>
            </nav>
            <div>Lance Fennell</div>
        </header>
        <main>
            {% block content %}{% endblock %}
        </main>
        <footer>
        </footer>
    </body>
</html>